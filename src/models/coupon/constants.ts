/**
 * Possible values:
 * - `1` - `Active`
 * - `2` - `Inactive`
 * - `3` - `Expired`
 * - `-3` - `Deleted`
 */
export enum CouponStatus {
    Active = 'Active',
    Inactive = 'Inactive',
    Expired = 'Expired',
    Deleted = 'Deleted',
}
