import {
    createPrice,
    deletePrice,
    generateCheckoutLink,
    getPrice,
    getPrices,
    updatePrice,
} from './api';

export * from './types';

export const price = {
    createPrice,
    deletePrice,
    generateCheckoutLink,
    getPrice,
    getPrices,
    updatePrice,
};
