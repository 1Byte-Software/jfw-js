/**
 * Possible values:
 * - `0` - `Function`
 * - `1` - `Limitation`
 */
export enum FeatureType {
    Function = 'Function',
    Limitation = 'Limitation',
}

/**
 * Possible values:
 * - `0` - `Inactive`
 * - `1` - `Active`
 */
export enum FeatureStatus {
    Inactive = 'Inactive',
    Active = 'Active',
}
