import { REST as REST_ANALYTIC } from '../paths';

const REST_PAYMENT = 'payments';

export const ANALYTIC_PAYMENT_PATH = {
    GET_SUMMARY_PAYMENTS: `v1/${REST_ANALYTIC}/${REST_PAYMENT}/summary`,
};
