/**
 * Possible values:
 * - `0` - `Inactive`
 * - `1` - `Active`
 */
export enum EventStatus {
    Inactive = 'Inactive',
    Active = 'Active',
}

/**
 * Possible values:
 * - `0` - `Inactive`
 * - `1` - `Active`
 */
export enum PhoneTemplateStatus {
    Inactive = 'Inactive',
    Active = 'Active',
}
